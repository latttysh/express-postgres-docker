# Используется базовый образ Node.js версии 22
FROM node:22  

# Устанавливается рабочая директория внутри контейнера на /server
WORKDIR /server  

# Копируется файл package.json в рабочую директорию контейнера
COPY package.json . 

# Устанавливаются зависимости, указанные в package.json
RUN npm install  

# Копируются все файлы и директории из текущей директории на хосте в рабочую директорию контейнера
COPY . . 

# Устанавливается команда, которая будет выполнена при запуске контейнера (запуск приложения с помощью npm start)
CMD npm start  